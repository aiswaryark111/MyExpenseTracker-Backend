name: Keep Lambda Warm

on:
  schedule:
    - cron: '*/5 * * * *' # every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Lambda
        run: |
          curl -s https://1idkxybara.execute-api.eu-west-1.amazonaws.com/prod/auth/me
          echo "Lambda pinged ✅"
